/**
 * Automatically generated by HugSql 🤗
 * {{ date }}
 */

const util = require('util');

module.exports = (db) => ({
{{#sections}}
{{&docstring}}
{{functionName}}: ({{parameters}}) => {
    const query = `{{&query}}`;
    const parameters = [{{sortedParameters}}];
    const promisify = util.promisify(db.all.bind(db));
    return promisify(query, parameters);
},

{{/sections}}
})
