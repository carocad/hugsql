
const statements = {
{{#sections}}
{{&docstring}}
{{functionName}}: ({{parameters}}) => ({
    query: `{{&query}}`,
    parameters: [{{sortedParameters}}]
}),

{{/sections}}
}

module.exports = statements
